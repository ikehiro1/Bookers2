<div class='container px-5 px-sm-0'>
    <div class="row">
       <%= render "layouts/info", user: @user, book: @book %>
      <div class="col-md-8 offset-md-1">
        <h2>Bookssssss</h2>
          <table class = "table table-hover table-inverse">
            <thead>
                <th></th>
                <th>Title</th>
                <th>Opinion</th>
                <th colspan="3"></th>
            </thead>
            <% @books.each do |book| %>
            <tr>
                <td><%= link_to user_path(book.user) do %>
                <%= link_to attachment_image_tag(@user, :profile_image, :fill, 60, 60, format: 'jpeg', fallback: "no_image.jpg"),user_path(@user.id) %><br>
                <% end %>
              </td>
              <td><%= link_to book.title,book_path(book.id) %></td>
              <td><%= book.body %></td>
              <% end %>
            </tr>
        </table>
      </div>
    </div>
  </div>